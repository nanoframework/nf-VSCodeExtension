# Copyright (c) .NET Foundation and Contributors
# See LICENSE file in the project root for full license information.

trigger:
  branches:
    include:
      - main
      - develop
      - release-*
  paths:
    exclude:
      - .github_changelog_generator
      - .gitignore
      - .markdownlint.json
      - CHANGELOG.md
      - LICENSE.md
      - README.md
      - assets/*
      - config/*
      - .github/*
      - .vscodeignore
      - .gitmodules
  tags:
    include: 
      - v*  
      - refs/tags/v*

# PR always trigger build
pr:
  autoCancel: true

# add nf-tools repo to resources (for Azure Pipelines templates)
resources:
  repositories:
    - repository: templates
      type: github
      name: nanoframework/nf-tools
      endpoint: nanoframework

pool:
  vmImage: 'windows-latest'

variables:
  - group: sign-client-credentials
  - name: DOTNET_NOLOGO
    value: true

steps:

  # need this here in order to persist GitHub credentials
  - checkout: self
    persistCredentials: true

  - pwsh: scripts/build.ps1
    displayName: Download and build dependencies for .NET nanoFramework

  - task: DeleteFiles@1
    displayName: Remove source folders
    condition: succeeded()
    inputs:
      Contents: |
        v*.zip

  - script: npm ci --legacy-peer-deps
    displayName: npm ci
    condition: succeeded()

  - script: npx gulp build-debug-bridge
    displayName: Build debug bridge
    condition: succeeded()

  # we don't have unit tests
  # - script: npm run test
  #   displayName: Run unit tests
  #   condition: succeeded()
    
  - script: npm run build:prod
    displayName: Build for production
    condition: succeeded()

  # Package as preview (pre-release) for develop branch and PRs targeting develop
  - script: npm run package:preview
    env:
      PublicRelease: true
    displayName: Package extension (preview)
    condition: >-
      and(
        succeeded(),
        or(
          eq(variables['Build.SourceBranch'], 'refs/heads/develop'),
          eq(variables['System.PullRequest.TargetBranch'], 'refs/heads/develop'),
          eq(variables['System.PullRequest.TargetBranch'], 'develop')
        )
      )

  # Package as stable release for tags and other branches
  - script: npm run package
    env:
      PublicRelease: true
    displayName: Package extension
    condition: >-
      and(
        succeeded(),
        ne(variables['Build.SourceBranch'], 'refs/heads/develop'),
        ne(variables['System.PullRequest.TargetBranch'], 'refs/heads/develop'),
        ne(variables['System.PullRequest.TargetBranch'], 'develop')
      )
  
  - task: DotNetCoreCLI@2
    displayName: Install Sign Client CLI
    condition: >-
      and(
        succeeded(),
        or(
          startsWith(variables['Build.SourceBranch'], 'refs/tags/v'),
          eq(variables['Build.SourceBranch'], 'refs/heads/develop')
        )
      )
    inputs:
      command: custom
      custom: tool
      arguments: install --tool-path . sign --version 0.9.1-beta.24312.3
  
  - pwsh: |
      .\sign code azure-key-vault `
      "**/*.vsix" `
      --base-directory "$(Build.Repository.LocalPath)" `
      --file-list "$(Build.Repository.LocalPath)\config\filelist.txt" `
      --description ".NET nanoFramework VS Code Extension" `
      --description-url "https://github.com/$env:Build_Repository_Name" `
      --azure-key-vault-tenant-id "$(SignTenantId)" `
      --azure-key-vault-client-id "$(SignClientId)" `
      --azure-key-vault-client-secret "$(SignClientSecret)" `
      --azure-key-vault-certificate "$(SignKeyVaultCertificate)" `
      --azure-key-vault-url "$(SignKeyVaultUrl)" `
      --timestamp-url http://timestamp.digicert.com
    displayName: Sign packages
    continueOnError: true
    condition: >-
      and(
        succeeded(),
        or(
          startsWith(variables['Build.SourceBranch'], 'refs/tags/v'),
          eq(variables['Build.SourceBranch'], 'refs/heads/develop')
        )
      )
  
  # Publish stable release from tags
  - bash: |
      echo ">>> Publish stable release"
      npm run deploy
    displayName: Publish to marketplace (stable)
    condition: >-
      and(
        succeeded(),
        startsWith(variables['Build.SourceBranch'], 'refs/tags/v')
      )
    env:
      VSCE_PAT: $(VSCE_PAT)

  # Publish preview release from develop branch
  - bash: |
      echo ">>> Publish preview release"
      npm run deploy:preview
    displayName: Publish to marketplace (preview)
    condition: >-
      and(
        succeeded(),
        eq(variables['Build.SourceBranch'], 'refs/heads/develop'),
        eq(variables['System.PullRequest.PullRequestId'], '')
      )
    env:
      VSCE_PAT: $(VSCE_PAT)

  - task: CopyFiles@2
    displayName: Collecting deployable artifacts
    condition: succeeded()
    inputs:
      sourceFolder: $(Build.SourcesDirectory)
      contents: |-
        **\vscode-nanoframework.vsix
        **\package.json
        assets/marketplace/vscode-marketplace-overview.md
        assets/marketplace/nano-vs-code.gif
      targetFolder: $(Build.ArtifactStagingDirectory)
      flattenFolders: true
    
  # Publish files as an artifact
  - task: PublishBuildArtifacts@1
    displayName: Publish deployables artifacts
    condition: succeeded()
    inputs:
      ArtifactName: git-mob-vs-code
      PathtoPublish: $(Build.ArtifactStagingDirectory)
      ArtifactType: Container

  # create or update GitHub release (stable, from main branch)
  - task: GithubRelease@1
    condition: >-
      and(
        succeeded(),
        eq(variables['System.PullRequest.PullRequestId'], ''),
        ne(variables['StartReleaseCandidate'], true),
        not(startsWith(variables['Build.SourceBranch'], 'refs/tags/v')),
        ne(variables['Build.SourceBranch'], 'refs/heads/develop')
      )
    displayName: Create/Update GitHub release
    inputs:
      gitHubConnection: 'github.com_nano-$(System.TeamProject)'
      tagSource: userSpecifiedTag
      tag: v$(NBGV_SimpleVersion)
      title: '.NET nanoFramework VS Code extension v$(NBGV_SimpleVersion)'
      assets: '$(Build.ArtifactStagingDirectory)/*.vsix'
      isPreRelease: false
      addChangeLog: true
      releaseNotesSource: inline
      changeLogType: issueBased
      changeLogLabels: |
        [
          { "label" : "Type: bug", "displayName" : "Bugs fixed", "state" : "closed" },
          { "label" : "Type: enhancement", "displayName" : "Enhancements and new features", "state" : "closed" },
          { "label" : "Breaking-Change", "displayName" : "Breaking Changes", "state" : "closed" },
          { "label" : "Type: dependencies", "displayName" : "Dependencies updated", "state" : "closed" },
          { "label" : "Type: documentation", "displayName" : "Documentation", "state" : "closed" }
        ]

  # create or update GitHub pre-release (preview, from develop branch)
  - task: GithubRelease@1
    condition: >-
      and(
        succeeded(),
        eq(variables['System.PullRequest.PullRequestId'], ''),
        eq(variables['Build.SourceBranch'], 'refs/heads/develop')
      )
    displayName: Create/Update GitHub pre-release
    inputs:
      gitHubConnection: 'github.com_nano-$(System.TeamProject)'
      tagSource: userSpecifiedTag
      tag: v$(NBGV_SimpleVersion)-preview
      title: '.NET nanoFramework VS Code extension v$(NBGV_SimpleVersion) (Preview)'
      assets: '$(Build.ArtifactStagingDirectory)/*.vsix'
      isPreRelease: true
      addChangeLog: true
      releaseNotesSource: inline
      changeLogType: issueBased
      changeLogLabels: |
        [
          { "label" : "Type: bug", "displayName" : "Bugs fixed", "state" : "closed" },
          { "label" : "Type: enhancement", "displayName" : "Enhancements and new features", "state" : "closed" },
          { "label" : "Breaking-Change", "displayName" : "Breaking Changes", "state" : "closed" },
          { "label" : "Type: dependencies", "displayName" : "Dependencies updated", "state" : "closed" },
          { "label" : "Type: documentation", "displayName" : "Documentation", "state" : "closed" }
        ]

  # report error
  - template: azure-pipelines-templates/discord-webhook-task.yml@templates  
    parameters:
      status: 'failure'
      webhookUrl: '$(DiscordWebhook)'
      message: ''
